<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Wizard View -->
    <record id="view_payment_request_wizard_form" model="ir.ui.view">
        <field name="name">payment.request.wizard.form</field>
        <field name="model">payment.request.wizard</field>
        <field name="arch" type="xml">
            <form string="Payment Request">
                <group>
                    <group>
                        <field name="amount"/>
                        <field name="late_fee"/>
                        <field name="total_amount"/>
                    </group>
                    <group>
                        <field name="payment_date"/>
                        <field name="due_date"/>
                        <field name="lease_id"/>
                        <field name="property_id"/>
                    </group>
                </group>    
                
                <footer>
                    <button name="submit_payment_request" string="Submit Payment" type="object" class="oe_highlight"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_payment_request_wizard" model="ir.actions.act_window">
        <field name="name">Payment Request</field>
        <field name="res_model">payment.request.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

</odoo>